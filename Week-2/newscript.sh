# on the first line of our script, we must specify which interpreter we would like to use to parse our script. In this case, it is bash.

#!/bin/bash
echo "Hello World!"
mkdir automated_dir
cd automated_dir && touch newscript_file2.txt
